server:
  port: 8080

spring:
  data:
    mongodb:
      uri: mongodb://admin:admin123@localhost:27017/franchise_db?authSource=admin

# Swagger configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha